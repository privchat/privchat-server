// use async_trait::async_trait;

// #[async_trait]
// pub trait NotificationService {
//     async fn send_notification(&self, user_id: &str, message: &str);
//     async fn get_notifications(&self, user_id: &str, limit: usize, offset: usize);
//     async fn mark_notification_as_read(&self, user_id: &str, notification_id: &str);
// }